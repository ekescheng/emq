---
- name: register ipv4
  shell: ip a | grep -w "inet" | grep ens33 |sed 's/^.*inet //g'|sed 's/\/[0-9][0-9].*$//g'
  register: nodeip
  changed_when: "nodeip.stdout != 0"
- name: change ip sed
  shell: sed -i 's#nodeip#{{ nodeip['stdout'] }}#g' /etc/emqttd/emq.conf
  changed_when: False
  notify: restart emq
